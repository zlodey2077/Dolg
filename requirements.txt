Django==6.0.6
# 2026-06-01 v22: production-сервер для Render/Heroku/любой PaaS
gunicorn==23.0.0
# WhiteNoise — отдача статики из Django без nginx (для Render free tier)
whitenoise==6.11.0
# 12-factor DATABASE_URL парсер (Render даёт postgres:// строку)
dj-database-url==2.3.0
# psycopg2-binary — драйвер Postgres для production. На dev — SQLite,
# psycopg2 не загружается. binary-версия не требует libpq-dev.
psycopg2-binary==2.9.10
asgiref==3.11.1
channels==4.3.2
channels-redis==4.3.0
daphne==4.2.1
celery==5.6.3
redis==7.4.1
django-allauth==65.16.1
django-otp==1.7.0
# Тема админки (drop-in AdminLTE: сайдбар-навигация, поиск, иконки, тёмная тема).
# Активация: 'jazzmin' в INSTALLED_APPS ПЕРЕД django.contrib.admin + Dolg_PR/jazzmin_config.py.
django-jazzmin==3.0.4
oauthlib==3.3.1
pyjwt==2.13.0
qrcode==8.2
rapidfuzz==3.14.5
# Phase 2 семантический поиск (TF-IDF через sklearn).
# Изначально планировался fastembed (ONNX), но на Windows + Python 3.14
# его транзитивная зависимость `py-rust-stemmers` требует Rust toolchain
# (нет prebuilt wheel). sklearn TF-IDF — pure Python+numpy/scipy, мгновенная
# установка, достаточно для дипломного каталога 100-1000 товаров.
# Опционально — без sklearn api_ai_find_analogs автоматически фолбекается
# на rule-based feature-векторы (Dolg_APP/ml/pipeline.py).
scikit-learn
attrs==26.1.0
certifi==2026.2.25
charset-normalizer==3.4.7
coverage==7.13.5
et_xmlfile==2.0.0
ezdxf==1.4.4
flexcache==0.3
flexparser==0.4
greenlet==3.5.0
idna==3.15
ImageHash==4.3.2
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
lark==1.3.1
matplotlib==3.10.9
networkx==3.6.1
numpy==2.4.5
olefile==0.47
openpyxl==3.1.5
pandas==3.0.3
pillow==12.2.0
pint==0.25.3
platformdirs==4.9.6
playwright==1.59.0
PyMuPDF==1.27.2.3
pdfplumber==0.11.9
pypdf==6.15.0
pyee==13.0.1
pytest==9.0.3
pymorphy3==2.0.6
pymorphy3-dicts-ru==2.4.417150.4580142
pytest-cov==7.1.0
pytest-django==4.12.0
python-docx==1.1.2
python-engineering==0.0.3
python-pptx==1.0.2
PyWavelets==1.9.0
referencing==0.37.0
reportlab==4.4.10
requests==2.33.1
rpds-py==0.30.0
ruff==0.15.13
rule-engine==5.0.0
schemdraw==0.22
scikit-fuzzy==0.5.0
scipy==1.17.1
sqlparse==0.5.5
stripe==15.0.1
sympy==1.14.0
typing_extensions==4.15.0
tzdata==2026.1
urllib3==2.7.0
# .env подгрузка — settings.py загружает HF_TOKEN, SECRET_KEY и т.п.
# из BASE_DIR/.env при старте. Без него env-vars нужно задавать в shell.
python-dotenv==1.2.2
z3-solver==4.16.0.0

# ─── Observability / monitoring stack ──────────────────────────────
# Sentry — error tracking. settings.py:326-340 уже умеет инициализировать
# если есть SENTRY_DSN env-var. Без sdk код try/except'ит, но reporting спит.
sentry-sdk==2.45.0
# Prometheus exporter — отдаёт /metrics/, который Prometheus собирает,
# а Grafana визуализирует. Docker-compose в deploy/ поднимает оба сервиса.
django-prometheus==2.4.1
# Runtime process metrics for staff monitoring: RSS/CPU/threads/disk snapshots.
psutil==7.2.2
# Django Silk — профайлер SQL-запросов и запросов (dev/staging). В production
# выключается через middleware-флаг.
django-silk==5.5.0

# ─── Security middleware ───────────────────────────────────────────
# brute-force protection на /login/, /2fa/, allauth. Auto-блок после N
# неудачных попыток за окно.
django-axes==7.0.2
# Content Security Policy headers — защита от XSS. Конфиг CSP_DEFAULT_SRC.
django-csp==4.0

# ─── API framework ─────────────────────────────────────────────────
# REST endpoints с serializers, permissions, OpenAPI docs. Постепенная
# миграция с raw JsonResponse-ов в Dolg_APP/views.py.
djangorestframework==3.16.1

# ─── Dev: типизация и хуки ─────────────────────────────────────────
# Не runtime — отдельно через requirements-dev.txt? Пока сюда.
mypy==1.18.2
django-stubs==5.2.5
pre-commit==4.6.0
# jurigged — hot-reload: правки тел функций применяются в живом процессе
# за <1с без рестарта (manual: start_server.py --local --hot). Работает на Py3.14 (reloadium —
# только <=3.11). Структурные правки (URL/model/settings) всё равно требуют рестарт.
jurigged==0.6.1
# pystray — трей-лаунчер dolg_tray.py (DOLG.vbs): управление dev-сервером из
# системного трея без консоли (старт/стоп/открыть). Pillow уже есть (иконка).
pystray==0.19.5

# ─── РЭБ-killer feature ────────────────────────────────────────────
# RF-анализ: S-параметры, Smith chart, передаточные функции. Опциональный
# слой для аналитики РЭБ-схем (антенны, фильтры, согласование импедансов).
scikit-rf==1.10.0

# Optional PyTorch neural backend:
#   pip install -r requirements-ai.txt
