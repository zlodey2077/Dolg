# Optional AI backend для DOLG Self AI deep-hints + расширения training dataset.
# Держим отдельно от requirements.txt, чтобы обычный Django startup/install
# оставался лёгким (без torch / parquet).

# Neural backend
torch==2.12.0

# External circuit datasets (опционально — для расширения training base).
# Используется в `python manage.py import_external_datasets` для подгрузки
# открытых КиCad-схем с HuggingFace, SPICE-netlist'ов с GitHub и т.п.
# Размер на диске после установки: ~250 МБ. Реальные данные качаются
# по требованию (`--source open_schematics --limit N`).
#
# Источники см. knowledge/notes/circuit_datasets.md:
#   - bshada/open-schematics (HF, CC-BY-4.0, 84 470 KiCad-схем)
#   - Masala-CHAI (academic, 7 500 SPICE-netlists)
#   - AnalogGym (academic, 30 топологий)
# Python 3.14: dill<0.3.9 (которое требует datasets 3.x) несовместимо с
# Pickler API py3.14 — падает с `_batch_setitems() takes 2 positional args`.
# Нужны datasets>=4.x (там dill без верхнего bound) + dill>=0.4 + multiprocess>=0.70.17.
datasets>=4.0
dill>=0.4.0
multiprocess>=0.70.17
pyarrow>=14.0
huggingface-hub>=0.20

# ─── Видео → конспект (YouTube транскрипты) ────────────────────────
# scripts/yt_transcript.py (CLI) + scripts/yt_mcp_server.py (MCP «yt-transcript»
# в ~/.claude/mcp.json). Движок: youtube-transcript-api (быстрые сабы) +
# yt-dlp фолбэк (метаданные/авто-сабы). Для анализа обучающих видео по проекту.
yt-dlp>=2026.3
youtube-transcript-api>=1.0
